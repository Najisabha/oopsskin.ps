# Node 22.13+; use an absolute persistent path when deploying.
DATABASE_PATH=./data/store.sqlite
# Optional: public app origin when running behind a reverse proxy.
# APP_ORIGIN=https://your-domain.com
# Products are local for now. Supplier URL/credentials will be added once provided.
