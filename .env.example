# MongoDB connection string (Atlas or self-hosted). Include the database name in the path.
MONGODB_URI=mongodb://127.0.0.1:27017/oopsskin
# Optional: public app origin when running behind a reverse proxy.
# APP_ORIGIN=https://your-domain.com
# Server-only Hsabate production credentials
HSABATE_EMAIL=
HSABATE_PASSWORD=
