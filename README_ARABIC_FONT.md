# 🎯 دليل تثبيت خط عربي لـ PDF

## المشكلة
خط `DejaVu Sans` المدمج في DomPDF لا يدعم النصوص العربية بشكل كامل، مما يؤدي إلى ظهور الأحرف منفصلة وغير متصلة.

## الحل
تثبيت خط **Cairo** الذي يدعم العربية بشكل ممتاز.

---

## 📥 الخطوة 1: تحميل خط Cairo

### خيار 1: من Google Fonts (مُوصى به)
1. افتح الرابط: https://fonts.google.com/specimen/Cairo
2. اضغط على زر **"Download family"**
3. استخرج الملف المضغوط

### خيار 2: تحميل مباشر
قم بتحميل الملفات التالية:
- `Cairo-Regular.ttf`
- `Cairo-Bold.ttf`

---

## 📁 الخطوة 2: نسخ الملفات

انسخ ملفات الخط (.ttf) إلى مجلد المشروع:

```
storage/fonts/Cairo-Regular.ttf
storage/fonts/Cairo-Bold.ttf
```

**ملاحظة:** إذا لم يكن مجلد `storage/fonts` موجوداً، سيتم إنشاؤه تلقائياً.

---

## ⚙️ الخطوة 3: تثبيت الخط

بعد نسخ الملفات، قم بتنفيذ أحد الأوامر التالية:

### الطريقة الأولى: استخدام أمر Artisan (مُوصى به)
```bash
php artisan font:install-arabic Cairo
```

### الطريقة الثانية: استخدام السكربت المباشر
```bash
php install-cairo-font.php
```

---

## ✅ الخطوة 4: التحقق

بعد التثبيت بنجاح، سيتم استخدام خط Cairo تلقائياً في الفواتير لأننا أضفناه كخيار أول في CSS.

يمكنك أيضاً مسح الكاش:
```bash
php artisan cache:clear
```

---

## 🎉 النتيجة

بعد التثبيت، ستظهر النصوص العربية بشكل صحيح ومتصلة في ملفات PDF.

---

## ❓ حل المشاكل

### المشكلة: "لم يتم العثور على ملف الخط"
- تأكد من نسخ الملفات إلى المجلد الصحيح: `storage/fonts/`
- تأكد من أن أسماء الملفات صحيحة: `Cairo-Regular.ttf` و `Cairo-Bold.ttf`

### المشكلة: "Permission denied"
- تأكد من أن مجلد `storage/fonts` قابل للكتابة
- تأكد من أن مجلد `vendor/dompdf/dompdf/lib/fonts` قابل للكتابة

### المشكلة: الخط لا يزال لا يعمل
- امسح الكاش: `php artisan cache:clear`
- تأكد من أن الخط مثبت بشكل صحيح في `vendor/dompdf/dompdf/lib/fonts/`

---

## 📝 ملاحظات
- خط Cairo مجاني ومفتوح المصدر
- يدعم العربية بشكل ممتاز مع RTL
- متوافق مع DomPDF
